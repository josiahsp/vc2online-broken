<p id="notice"><%= notice %></p>

<p>
  <strong>Campus:</strong>
  <%= @meeting.location.title.titlecase %>
</p>

<p>
  <strong>Title:</strong>
  <%= @meeting.service.title %>
</p>

<p>
  <strong>Weekday:</strong>
  <%= Date::DAYNAMES[@meeting.weekday] %>
</p>

<p>
  <strong>Time:</strong>
  <%= @meeting.time.strftime("%I:%M %p") %>
</p>

<p>
  <strong>Display in Locations Partial:</strong>
  <%= @meeting.level == 1 ? "Yes" : "No" %>
</p>

<%= link_to 'Edit', edit_meeting_path(@meeting) %> |
<%= link_to 'Back', meetings_path %>
